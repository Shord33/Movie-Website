let rain = true
if(rain){
    console.log("dont go")
}
else{
    console.log("go")
}

//ternary operators
// operand1 ? operand2 : operand3;
//operand 1 = value to be determined, if true operand2 executed, else operand3

let travel = rain ? "dont go" : "go" //shorter code, ONLY 2 cconditions
travel

